# ===========================================
# VISUAL PRODUCT MATCHER - BACKEND CONFIG
# ===========================================

# Server Configuration
NODE_ENV=development
PORT=5000

# MongoDB Database
MONGODB_URI=mongodb://localhost:27017/product-matcher

# ===========================================
# CLIP Service (Recommended - 512 dimensions)
# ===========================================
# Enable local CLIP service for high-quality visual embeddings
USE_LOCAL_CLIP=true
LOCAL_CLIP_URL=http://localhost:3000

# ===========================================
# Hugging Face API (Fallback - 5 dimensions)
# ===========================================
# Only used when CLIP service is unavailable
# Get your API key from: https://huggingface.co/settings/tokens
HUGGINGFACE_API_KEY=your_huggingface_api_key_here
HUGGINGFACE_MODEL=google/vit-base-patch16-384

# ===========================================
# Security & Performance
# ===========================================
# Frontend URL for CORS (production)
FRONTEND_URL=http://localhost:3001

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# ===========================================
# Production Deployment Notes
# ===========================================
# 1. Set NODE_ENV=production in production
# 2. Use strong MongoDB credentials
# 3. Update FRONTEND_URL to your production domain
# 4. Ensure CLIP service is running for best results
# 5. Never commit .env files to version control
